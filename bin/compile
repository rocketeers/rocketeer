#!/usr/bin/env php
<?php
require __DIR__.'/../vendor/autoload.php';

$phar = __DIR__.'/rocketeer.phar';

// Create Phar
$compiler = new Rocketeer\Console\Compiler;
$compiler->compile($phar);

// Set permissions
chmod($phar, 0755);